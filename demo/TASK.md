Demo task (copy into --task):

The test suite in this repo fails. Run `python -m pytest -q` to see the failures, then implement the
`--pending` and `--done` filter flags for the `list` command in todo.py so that all tests pass.
Do not modify the tests.
