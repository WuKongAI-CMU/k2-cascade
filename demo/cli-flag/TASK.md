Demo task (copy into --task):

The test suite in this repo fails. Run `python -m pytest -q` to see the failures, then add a
`--chars` flag (print the character count) and a `--top N` option (print the N most common words
as `word count` lines) to wc.py so that all tests pass. Do not modify the tests.
